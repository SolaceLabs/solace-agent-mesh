/**
 * Re-export from the new React Query-based module.
 * Kept for backwards compatibility with existing imports.
 */
export { useAgentCards } from "@/lib/api/agent-cards/hooks";
export { transformAgentCard } from "@/lib/api/agent-cards/service";

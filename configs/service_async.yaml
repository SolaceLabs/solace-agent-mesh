# This is the configuration file for the Async Service.
#
# The Async Service handles asynchronous tasks for human-in-the-loop interactions.
# It will subscribe to async service events and manage task groups and tasks.
#
# The general topic format is:
# - <namespace>/solace-agent-mesh/v1/stimulus/async-service/<verb>/<stimulus-uuid>/<session-uuid>/<originator-id>
#
# The specific subscriptions are:
# - <namespace>/solace-agent-mesh/v1/stimulus/async-service/create-task-group/>
# - <namespace>/solace-agent-mesh/v1/stimulus/async-service/user-response/>
# - <namespace>/solace-agent-mesh/v1/stimulus/async-service/get-pending-forms/>

---
log:
  stdout_log_level: INFO
  log_file_level: INFO
  log_file: solace_agent_mesh.log

shared_config:
  - broker_config: &broker_connection
      broker_connection_share: ${SOLACE_BROKER_URL}
      dev_mode: ${SOLACE_DEV_MODE, false}
      broker_url: ${SOLACE_BROKER_URL}
      broker_username: ${SOLACE_BROKER_USERNAME}
      broker_password: ${SOLACE_BROKER_PASSWORD}
      broker_vpn: ${SOLACE_BROKER_VPN}
      payload_encoding: utf-8
      payload_format: json
      temporary_queue: ${USE_TEMPORARY_QUEUES, false}

flows:
  # Handling async service requests
  - name: async-service
    components:
      - component_name: broker_input
        component_module: broker_input
        component_config:
          <<: *broker_connection
          broker_queue_name: ${SOLACE_AGENT_MESH_NAMESPACE}solace-agent-mesh/v1/async-service
          broker_subscriptions:
            - topic: ${SOLACE_AGENT_MESH_NAMESPACE}solace-agent-mesh/v1/stimulus/async-service/create-task-group/>
              qos: 1
            - topic: ${SOLACE_AGENT_MESH_NAMESPACE}solace-agent-mesh/v1/stimulus/async-service/user-response/>
              qos: 1
            - topic: ${SOLACE_AGENT_MESH_NAMESPACE}solace-agent-mesh/v1/stimulus/async-service/get-pending-forms/>
              qos: 1

      - component_name: async_service_component
        component_module: src.services.async_service.async_service_component
        component_config:
          storage_provider: ${ASYNC_SERVICE_STORAGE_PROVIDER, memory}
          mongodb_connection_string: ${MONGODB_CONNECTION_STRING, mongodb://localhost:27017/}
          mongodb_username: ${MONGODB_USERNAME, ""}
          mongodb_password: ${MONGODB_PASSWORD, ""}
          task_timeout: ${ASYNC_TASK_TIMEOUT, 3600}
        input_selection:
          source_expression: input.payload

      # Add iterate component to handle multiple messages
      - component_name: split_events
        component_module: iterate

      - component_name: broker_output
        component_module: broker_output
        component_config:
          <<: *broker_connection
          copy_user_properties: false
        input_selection:
          source_expression: previous
  
  # Handling spontaneous send events
  - name: send-spontaneous-message
    components:
      - component_name: broker_output
        component_module: broker_output
        component_config:
          <<: *broker_connection
          copy_user_properties: true
        input_transforms:
          - type: copy
            source_expression: previous:payload
            dest_expression: user_data.output:payload
          - type: copy
            source_expression: previous:topic
            dest_expression: user_data.output:topic
          - type: copy
            source_expression: previous:user_properties
            dest_expression: user_data.output:user_properties
        input_selection:
          source_expression: user_data.output

# Outstanding issues

## How do we work with session history?
- When we suspend the stimulus, do we update gateway to say that the last request is still pending?
- Do we update the session timeout, or let it disappear?
- When we resume, do we update the history with the result?
- If the result takes a day, do we re-populate the session history?

## If the user is in a different channel or thread, can they continue to use Solace Chat while the request is pending? In the same session?

## What if a component is upgraded during the approval process and it has a different I/F or behaviour
? versioned topics ?